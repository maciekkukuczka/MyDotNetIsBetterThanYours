@inherits AdminCollapseCardComponentBase


@if (Item == null)
{
    <div>Loading</div>
}
else
{
    @foreach (var item in Item.Answers)
    {
        <div class="card  collapse @(IsCollapsed ? "show" : "")">
            <div class="card-body d-flex justify-content-between">

                <div class="container-fluid">
                    <div class="row">
                        <div class="col-10">
                            <div>
                                @item.Content
                            </div>

                        </div>
                        <div class="col-2 d-flex justify-content-around align-items-center">
                            <div>@item.CreatedDate</div>

                            <button class="btn btn-outline-info" @onclick=OnClick>
                                Usuń
                            </button>

                        </div>


                    </div>


                </div>


                @* <div>@answer.User.IsActive</div> *@
                @* <div>@answer.User.Points</div> *@

            </div>
            @*                 @if (item!= Item.Answers.Last()) *@
            @*                 { *@
            @*                     <div class="dropdown-divider " style="margin: 0 1% ; border-top: 1px solid black; border-top-left-radius:50%; height: 10px;  "></div> *@
            @*                 } *@
        </div>
    }
}
<!-- Modal -->
@* @if (ModalIsOpen) *@
@* { *@
@*     <ModalAddAnswerComponent IsOpen="@ModalIsOpen" Model="@Item" ModelChanged="@SaveAsync"></ModalAddAnswerComponent> *@
@* } *@